<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mo.edu.ipm.cp.siweb">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".GradeAndAbsenseActivity"
            android:label="@string/title_activity_grade_and_absense"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".PublicDrive"
            android:label="@string/title_activity_public_drive"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AttendenceHistoryActivity"
            android:parentActivityName=".GradeAndAbsenseActivity" />
        <activity android:name=".ExamTimeActivity" />
        <activity android:name=".ClassTimeActivity"
            android:label="Class Time"
            android:parentActivityName=".MainActivity" ></activity>
    </application>

</manifest>